<div className="md:hidden px-5 py-8">
  {/* Section Header: Bold & Hyper-Local */}
  <div className="flex flex-col mb-2">
    <div className="flex items-center gap-2 mb-1">
      <MapPin className="w-3.5 h-3.5 text-[#FF7033]" fill="currentColor" />
      <span className="text-[15px] font-black uppercase tracking-[0.2em] text-orange-600">
        Near You • Pune
      </span>
    </div>
  </div>

  <section className="md:hidden px-5 py-8 bg-white/50 border-y border-slate-100">
    <div className="flex items-end justify-between mb-6">
      <div className="space-y-1.5">
        <div className="flex items-center gap-2">
          <span className="relative flex h-2 w-2">
            <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-orange-500 opacity-75"></span>
            <span className="relative inline-flex rounded-full h-2 w-2 bg-orange-600"></span>
          </span>
          <span className="text-[11px] font-black uppercase tracking-[0.2em] text-orange-600">
            Personalized Picks
          </span>
        </div>
        <h2 className="text-3xl font-black text-slate-900 tracking-tighter leading-none">
          Recommended
        </h2>
      </div>

      <Link to="/mess" className="group flex items-center gap-1 text-[12px] font-bold text-slate-400 hover:text-orange-600 transition-colors">
        View all
        <ChevronRight className="w-4 h-4 transition-transform group-hover:translate-x-0.5" />
      </Link>
    </div>

    {/* Recommendation List Logic */}
    
    <div className="space-y-4">
      {isLoading ? (
        /* 1. LOADING STATE: Skeleton Loader */
        [1, 2, 3].map((i) => (
          <div key={i} className="flex items-center p-3 bg-white rounded-[2rem] border border-slate-50 shadow-sm animate-pulse">
            <div className="h-20 w-20 rounded-[1.5rem] bg-slate-200 flex-shrink-0" />
            <div className="flex-1 ml-4 space-y-2">
              <div className="h-4 w-3/4 bg-slate-200 rounded" />
              <div className="h-3 w-1/2 bg-slate-100 rounded" />
              <div className="flex gap-2">
                <div className="h-4 w-12 bg-slate-100 rounded-full" />
                <div className="h-4 w-12 bg-slate-100 rounded-full" />
              </div>
            </div>
            <div className="h-10 w-10 rounded-full bg-slate-50 ml-1" />
          </div>
        ))
      ) : AllMESS && AllMESS.length > 0 ? (
        /* 2. DATA STATE: Using Dummy Thali Images */
        AllMESS.filter((mess)=>mess.messVerified ==  true).slice(0,3).map((mess, index) => (
          <div
            key={mess._id}
            onClick={() => navigate(`mess/${mess?._id}`)}
            className="group relative flex items-center p-3 bg-white rounded-[2rem] border border-slate-100 shadow-[0_10px_30px_-15px_rgba(0,0,0,0.08)] active:scale-[0.97] transition-all duration-300"
          >
            {/* Image Container */}
            <div className="relative h-20 w-20 rounded-[1.5rem] overflow-hidden flex-shrink-0 shadow-inner bg-slate-100">
              <img
                /* Cycles through DUMMY_THALIS array so images look different */
                src={mess?.media?.cover}
                className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-110"
                alt="Thali"
              />
              <div className="absolute inset-0 bg-black/5" />
              
              {/* Status Indicator */}
              <div className="absolute bottom-1 right-1 h-5 w-5 bg-white rounded-full flex items-center justify-center shadow-sm">
                <div className={cn(
                  "h-2.5 w-2.5 rounded-full",
                  mess?.identity?.dietaryType ? "bg-emerald-500" : "bg-orange-500"
                )} />
              </div>
            </div>

            {/* Content Area */}
            <div className="flex-1 ml-4 overflow-hidden">
              <div className="flex items-center gap-2 mb-1">
                <h4 className="font-extrabold text-slate-800 text-base tracking-tight truncate">
                  {mess?.identity?.name}
                </h4>
                <div className="flex items-center gap-0.5 bg-yellow-400/10 px-1.5 py-0.5 rounded-md">
                  <Star className="w-2.5 h-2.5 fill-yellow-500 text-yellow-500" />
                  <span className="text-[10px] font-black text-yellow-700">4.2</span>
                </div>
              </div>

              <div className="flex flex-col gap-1.5">
                <div className="flex items-center gap-1.5">
                  <MapPin className="w-3.5 h-3.5 text-slate-300" />
                  <span className="text-xs font-bold text-slate-500 truncate">
                    {calculateDistance(userlat, userlng, mess.location.lat, mess.location.lng)} km • Pune
                  </span>
                </div>
                <div className="flex gap-2">
                  <span className="text-[9px] font-black text-orange-600 bg-orange-50 px-2 py-0.5 rounded-full border border-orange-100 uppercase tracking-tighter">
                    Hygienic
                  </span>
                  <span className="text-[9px] font-black text-slate-400 bg-slate-50 px-2 py-0.5 rounded-full border border-slate-100 uppercase tracking-tighter">
                    Verified
                  </span>
                </div>
              </div>
            </div>

            {/* Arrow Button */}
            <div className="h-10 w-10 flex items-center justify-center rounded-full bg-slate-50 text-slate-300 group-active:bg-orange-600 group-active:text-white transition-all ml-1">
              <ArrowRight className="w-5 h-5 transition-transform group-hover:translate-x-0.5" />
            </div>
          </div>
        ))
      ) : (
        /* 3. EMPTY STATE */
        <div className="py-10 text-center bg-slate-50 rounded-[2rem] border border-dashed border-slate-200">
          <Utensils className="w-8 h-8 text-slate-300 mx-auto mb-2" />
          <p className="text-sm font-bold text-slate-400">No messes found near you yet.</p>
        </div>
      )}
    </div>

    {/* Discovery Button */}
    <Link to="/mess" className="block mt-8">
      <div className="relative h-16 rounded-2xl bg-black flex items-center justify-between px-6 overflow-hidden shadow-2xl shadow-orange-100">
        <div className="absolute right-0 top-0 h-full w-1/3 bg-[#FF7033] skew-x-[-20deg] translate-x-8" />
        <span className="relative z-10 text-white font-black text-sm uppercase tracking-wider">
          Find <span className="text-orange-500">Messes</span> Near You
        </span>
        <div className="relative z-10 h-8 w-8 bg-black rounded-full flex items-center justify-center">
          <ArrowRight className="w-5 h-5 text-orange-500" />
        </div>
      </div>
    </Link>
  </section>
</div>